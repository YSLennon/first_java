-- 유용한 함수(그룹 함수)
-- AVG: 평균, SUM
SELECT * FROM STUDENT;

-- 평균 (AVG)
SELECT AVG(STU_HEIGHT)
FROM STUDENT;

-- 합 (SUM)
SELECT SUM(PAY)
FROM T_EMP;

-- 최대값 (MAX)
SELECT MAX(PAY)
FROM T_EMP;

-- 최소값 (MIN)
SELECT MIN (PAY)
FROM T_EMP;





